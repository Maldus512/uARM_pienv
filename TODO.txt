- Correctly understand and setup memory share between CPU and GPU
- Set up every needed interrupt (uart, swi, ...)
- Set up JTAG debugging
- Set up caches and MMU
- Work with multiprocessors
- Try to use fillexp in linker script to zero the bss section instead of crt0